# Excluded legacy prototypes

`src/features/tasks` and `src/features/friends` are not imported by the active application. They intentionally remain outside `frontend/` until a separate review decides whether their ideas should be retained or removed.
